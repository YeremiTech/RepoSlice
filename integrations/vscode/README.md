# RepoSlice VS Code

The VS Code integration delegates workspace analysis to the local RepoSlice CLI through `execFile`. Source code remains local and the extension does not send repositories to an external service.

Available command: **RepoSlice: Analyze Workspace**.
